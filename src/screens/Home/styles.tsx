import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingTop: 100,
        justifyContent: 'flex-start',
        alignItems: 'center',
        backgroundColor: '#D8EEFF', // Adicionado o fundo azul aqui
    },
    flatList: {
        flex: 1,
        paddingHorizontal: 25,
    }
});